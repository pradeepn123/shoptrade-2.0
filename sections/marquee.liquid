<section class="homeMarque">
    <div class="marquee marquee--hover-pause enable-animation-banner">
        <div class="marquee__content">

            {% for block in section.blocks %}
            <div class="Marquee-tag">
                {% comment %} <img class="image01" src="{{ block.settings.imageDoller | image_url }}" alt="marguee one"/> {% endcomment %}
                <img class="image02" src="{{ block.settings.imageLogo | image_url }}" alt="marguee two" />
            </div>
        {% endfor %}
        </div>
        <div class="marquee__content aria-hidden="true"">
            {% for block in section.blocks %}
                <div class="Marquee-tag">
                    {% comment %} <img class="image01" src="{{ block.settings.imageDoller | image_url }}" alt="marguee one"/> {% endcomment %}
                    <img class="image02" src="{{ block.settings.imageLogo | image_url }}" alt="marguee two" />
                </div>
            {% endfor %}
        </div>
    </div>
    </div>
</section>

{% schema %}
{
    "name":"Marguee",
    "blocks":[
        {
            "name":"Marquee Image",
            "type":"marqueeType",
            "settings": [
               
                {
                    "type":"image_picker",
                    "label":"logo",
                    "id":"imageLogo"
                }
            ]

            
        }
       
    ]
}
{% endschema %}






